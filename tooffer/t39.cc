//二分查找的代码
#include<iostream>

using namespace std;

int binary_search(int list[], int left, int right, int number)
{
	if(list == nullptr)
	{
		return -1;
	}

	while(left < right){

		int mid = (right + left)/2;

		if(number > list[mid])
		{
			
		}

	}
}